// 배포 시 DB에 1회 자동 삽입할 초기 자료(refs).
// 각 항목의 marker 로 중복 삽입을 막는다 (migrations 테이블 가드).
// 새 시드를 추가하려면 여기에 항목을 추가하고 새 marker 를 부여하면 된다.

export const SEED_REFS = [
  {
    marker: "seed_ref_latent_ode_v1",
    project: "predictive",
    kind: "paper",
    title: "Latent + 로지스틱 ODE 3-파라미터 방식 (cWDM 대안)",
    url: null,
    body: `## 핵심 아이디어
원시 복셀 패턴을 직접 합성(컨볼루션/디퓨전)하는 대신, 입력을 **잠재(latent)** 로 임베딩하고 그 잠재에서 복셀(또는 ROI)별로 **궤적 파라미터 3개**를 뽑아 방정식으로 임의 시점 값을 생성한다.

## 파라미터 3개 (로지스틱 ODE)
- **y0** = 베이스라인 값 (SC 시점)
- **r** = 변화 기울기 / 속도
- **K** = 세츄레이션(포화) 지점 = 오버슈팅 방지 상/하한

        dy/dt = r·y·(1 - y/K)
        y(t)  = K / (1 + ((K - y0)/y0)·e^(-rt))

세 값만 있으면 아무 시점 t 의 값 합성 가능. 복셀별로 뽑으면 시점별 3D 영상 재구성 + 진행속도 지도.

## cWDM과의 차이
- **cWDM**: 원시 복셀 패턴을 직접 학습해 미래 영상을 통째로 생성.
- **이 방식**: 잠재에서 생물학적 의미 있는 3파라미터만 추정 → 방정식이 나머지.

장점: (1) 시간 연속(미관측 시점 보간), (2) 자유도 ↓ → 과적합·노이즈에 강함·단조 포화 강제, (3) "베이스라인 복사(identity)" 구조적 회피(r 명시), (4) 복셀별 파라미터 = 진행속도 지도.

## 파킨슨 적용
- 알츠하이머 아밀로이드는 증가형 S자 (K=상한). 파킨슨 **DAT/SBR은 감소형 → 뒤집힌 로지스틱** (K=하한).
- 입력: 베이스라인 DaTScan을 3D 인코더로 임베딩. **UPDRS/H&Y** 등 임상지표를 잠재에 같이 넣으면 파킨슨 특이 진행 방향이 더 잡힐 가능성.

## 함정 / 할 일
- DAT 감소가 실제 S자/포화형인지 데이터로 확인 (아니면 지수감소 등).
- 복셀별 3파라미터 = 파라미터 폭발 → 공간 평활 prior (L_smooth) 필수. **ROI 단위로 먼저 검증** 후 복셀로.
- 기울기·포화 학습엔 다시점 필요 (PPMI SC/V04/V06/V10). 모델이 "베이스라인→파라미터"를 인구 전체에서 amortize.
- 참고 아키텍처: 보경 워크플로우 v2의 **Logistic ODE Trajectory Decoder** (Parameter Prediction Heads → y0,K,r → constraints → ODE solver → 연속 궤적). 실제 코드 돌려보고 재검토.

> 출처: 2026-06-11 닷스캔 미팅`,
  },
];
